{
  "name": "redux-toolkit-compiler",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:development": "set NODE_ENV=development && set REDUX_FILE_FORMAT=esm && rollup -c",
    "build:production": "set NODE_ENV=production && set REDUX_FILE_FORMAT=esm && rollup -c",
    "build:developmentcjs": "set NODE_ENV=development && set REDUX_FILE_FORMAT=cjs && rollup -c",
    "build:productioncjs": "set NODE_ENV=production && set REDUX_FILE_FORMAT=cjs && rollup -c",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@reduxjs/toolkit": "^2.3.0",
    "@rollup/plugin-commonjs": "^28.0.1",
    "@rollup/plugin-node-resolve": "^15.3.0",
    "@rollup/plugin-replace": "^6.0.1",
    "@rollup/plugin-terser": "^0.4.4",
    "rollup": "^4.24.4"
  }
}
